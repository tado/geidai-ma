# Marp形式のMarkdownからWord Press用に変換

## 作業内容:

Marp形式のMarkdownをWord Press用のMarkdownに変換する。

## 変換ルール:

- 表紙のH1見出しを投稿のタイトルにする
- Marpのスライド区切り（`---`）を削除
- 画像は幅640pxに統一する

## 変換したファイルを保存

ファイル名は元のファイル名から「-marp」を削除して代わりに「-wp」を付けて保存する。例えば、`presentation-marp.md`は`presentation-wp.md`となる。